# Ревизия фреймворка SDX — находки и рекомендации (2026-07-01)

Результаты глубокой ревизии фреймворка: `CLAUDE.md`, `protocol.md`, 13 команд, 8 агентов,
5 хуков + их тесты (31/31 зелёные на момент ревизии), ADR-журнал, SPEC/DESIGN Фазы 1,
исходный бандл `sdx-efficiency-automation-2026`.

**Назначение документа:** бэклог для организации сессий доработок. Каждая находка имеет
ID (`A*` — enforcement, `B*` — дрейф документов, `C*` — процесс, `D*` — экономика),
приоритет и предлагаемый трек. Статус обновлять по мере закрытия.

**Общая оценка:** фреймворк зрелый и внутренне последовательный (адаптивные треки, контракт
изоляции ревьюера, safe-by-default enforcement, ADR-журнал). Найдены: один функциональный
дефект процесса (A1), несколько дыр enforcement-слоя, сработавшее обязательство по смене
поколения моделей (B1) и риск потери roadmap (C1).

---

## A. Дефекты и дыры enforcement-слоя

### A1. Stage-gate блокирует qa и developer на стадии Verification — КРИТИЧНО
- **Статус:** закрыто (сессия `fw-enforce-a1a2-20260703`, 2026-07-03) — выбрана предпочтительная модель: тестовые пути открыты на Verification, правки кода — через backtrack
- **Суть.** `stage-gate.sh` разрешает запись в код только на `Execution|Deployment`. Но по
  `/sdx:verify` шаг 2 агент `qa` пишет интеграционные тесты на стадии Verification (это его
  объявленная роль), а шаг 7 направляет `developer` исправлять FAIL-находки. Тестовые файлы
  (`tests/*` и т.п.) не подпадают под always-allow (`docs/*`, `.claude/*`, `*.md`) — хук их
  задержит. PreToolUse действует и на субагентов. В мета-проекте не всплыло (здесь всё `.md`),
  но на любом прикладном проекте full/standard верификация упрётся в собственный гейт.
- **Рекомендация.** Выбрать одну модель и зафиксировать в DESIGN/протоколе:
  - (предпочтительно) добавить `Verification` в разрешённые стадии для тестовых путей
    (встроенный allow `tests/**`, `test/**`, `spec/**`), а правки кода по FAIL-находкам
    по-прежнему требовать через `/sdx:backtrack --to Execution`;
  - либо декларировать, что все тесты пишутся на Execution, а `qa` на Verification только
    запускает и судит — тогда поправить `qa.md` и `verify.md`.
- **Затрагивает:** `stage-gate.sh`, `test-stage-gate.sh`, `qa.md`, `verify.md`,
  `protocol.md`, `docs/designs/phase1-enforcement-routing.md`.

### A2. Prod-guard fail-closed блокирует весь Bash даже без сконфигурированной защиты
- **Статус:** закрыто (сессия `fw-enforce-a1a2-20260703`, 2026-07-03) — проверка conf (с has_pattern) поднята выше jq; fail-closed только при сконфигурированной защите
- **Суть.** `prod-guard.sh` проверяет `jq` до проверки наличия `prod-guard.conf`. На проекте
  без прод-среды (conf отсутствует/пустой = «защиты нет по договорённости») отсутствие `jq`
  превращается в блокировку каждой Bash-команды, включая `ls`. Fail-closed оправдан только
  когда защита реально сконфигурирована.
- **Рекомендация.** Поднять проверку conf (`[ -f ... ] || exit 0`; лучше — «есть ли хоть один
  не-комментарий») выше jq-проверки — она jq не требует. Fail-closed остаётся для проектов
  с заполненным conf. Добавить тест.
- **Затрагивает:** `prod-guard.sh`, `test-prod-guard.sh`, `protocol.md` (описание деградации).

### A3. Весь enforcement держится на самодекларируемом поле `stage`
- **Статус:** открыто
- **Суть.** `stage-gate`/`stop-gate` читают `stage` из `session_state.json`, который лежит под
  `.claude/**` (always-allow). Модель, которую гейт ограничивает, может при деградации
  контекста записать `stage: Execution` в обход `/sdx:next` — и заморозка кода исчезает.
  Это тот самый класс «инвариант-в-прозе», который Фаза 1 объявила ненадёжным.
- **Рекомендация.** Детерминированный скрипт перехода `sdx-stage.sh <session> <new-stage>`,
  который сам проверяет гейт-артефакты (выход из Verification — только при существующем
  `verification_report.md` без FAIL и т.д.) и единственный пишет `stage`; плюс
  PreToolUse-deny на `Write|Edit` в `session_state.json` (правки состояния — только через
  скрипт по Bash). Закрывает случайный обход, не претендуя на защиту от намеренного.
- **Затрагивает:** новый хук/скрипт, `stage-gate.sh`, `next.md`, `retrack.md`,
  `backtrack.md`, `protocol.md`.

### A4. Stop-gate гоняет полный тест-сьют на каждом завершении хода
- **Статус:** закрыто (сессия `fw-econ-a4d1-20260703`, 2026-07-03) — green-run cache по отпечатку дерева (`HEAD` + porcelain) в `.stopgate.ok`, ADR-011
- **Суть.** На Execution/Verification каждый `Stop` запускает verify-команду (до 180 с).
  Длинная Execution-стадия из десятков ходов = десятки прогонов без изменений кода.
- **Рекомендация.** Кэшировать по состоянию дерева: сохранять в `.stopgate.ok` хэш
  (`git rev-parse HEAD` + хэш `git status --porcelain`/diff) последнего зелёного прогона и
  выходить `exit 0`, если дерево не изменилось.
- **Затрагивает:** `stop-gate.sh`, `test-stop-gate.sh`.

### A5. Обход stage-gate через Bash не зафиксирован как граница
- **Статус:** открыто
- **Суть.** У prod-guard граница честно задокументирована («покрывает только Bash»). У
  stage-gate симметричная дыра: `echo > file`, `sed -i`, `tee` через Bash пишут код на любой
  стадии — нигде не отмечено. Закрывать эвристиками по Bash-командам не стоит (хрупко).
- **Рекомендация.** Задокументировать границу в разделе «Enforcement-слой» `protocol.md` —
  по тому же стандарту честности, что у prod-guard.
- **Затрагивает:** `protocol.md`.

### A6. Мета-проект имеет тест-сьют, но не «доедает свой корм»
- **Статус:** открыто
- **Суть.** ADR-4 и SPEC исходят из «у мета-проекта нет тестов → stop-gate no-op». Уже
  неправда: в `.claude/sdx/hooks/` лежит 31 юнит-тест (`test-*.sh`), и ничто их не запускает
  автоматически.
- **Рекомендация.** Создать исполняемый `.claude/sdx/verify-cmd.sh` мета-проекта, запускающий
  `test-*.sh` — stop-gate станет активен на будущих SDX-сессиях фреймворка (dogfooding),
  регресс хуков будет ловиться детерминированно. Обновить примечание в `verify.md`
  («в мета-проекте stop-gate прозрачен» — устареет) и ADR-4.
- **Затрагивает:** `verify-cmd.sh` (новый), `verify.md`, `docs/DECISIONS.md`.

---

## B. Несоответствия и дрейф документов

### B1. Сработало обязательство обновить раскладку моделей на новое поколение
- **Статус:** закрыто (сессия `fw-model-aliases-20260702`, 2026-07-02) — переход на алиасы тиров, ADR-008
- **Суть.** DESIGN сознательно пиннит полные ID (`claude-sonnet-4-6`, `claude-opus-4-8`,
  `claude-haiku-4-5`); бандл фиксирует обязательство «при смене поколения обновить раскладку».
  Поколение сменилось: актуальная линейка — семейство Claude 5 (`claude-sonnet-5` и выше).
  Шесть агентов работают на Sonnet 4.6 при доступном Sonnet 5.
- **Рекомендация.** Отдельная сессия (standard): рабочие роли → `claude-sonnet-5`; для
  `reviewer` взвесить Opus 4.8 vs Sonnet 5 (новое поколение может давать сравнимое суждение
  дешевле). Альтернатива навсегда: перейти на алиасы (`sonnet`, `opus`, `haiku`) — схема
  поддерживает — и убрать класс проблемы «пин протух», приняв трейд-офф (меньше контроля
  над поколением). Решение зафиксировать в ADR.
- **Затрагивает:** frontmatter 8 агентов, `CLAUDE.md` §2, `architect.md` (политика эскалации).

### B2. Раскладка моделей продублирована в трёх местах
- **Статус:** закрыто (сессия `fw-model-aliases-20260702`, 2026-07-02) — в `CLAUDE.md` остался принцип без ID, источник истины — frontmatter
- **Суть.** Frontmatter агентов (источник истины), `CLAUDE.md` §2 (конкретные ID) и
  SPEC/DESIGN Фазы 1. При обновлении (B1) — три точки правки, `CLAUDE.md` гарантированно отстанет.
- **Рекомендация.** В `CLAUDE.md` оставить только принцип («фронтир — на оценочное суждение,
  дешёвые — на механику; раскладка — во frontmatter агентов»), без конкретных ID.
  Делать вместе с B1.
- **Затрагивает:** `CLAUDE.md` §2.

### B3. Противоречие verify.md ↔ reviewer.md: кто вычисляет diff
- **Статус:** открыто
- **Суть.** `/sdx:verify` шаги 4–5: оркестратор вычисляет diff и передаёт его ревьюеру как
  данные. `reviewer.md` инструкция 2: «Получи diff поставки целиком (`git diff ...`)» — т.е.
  ревьюер вычисляет сам через Bash. Второе хуже по токенам (diff дважды в контекстах) и по
  изоляции (Bash даёт ревьюеру доступ ко всему, включая `session.log` — стена держится на прозе).
- **Рекомендация.** Оркестратор материализует diff в файл редиректом
  (`git diff main...sdx/<id> > .claude/sessions/<id>/delivery.diff` — не через свой контекст)
  и передаёт путь; из `reviewer.md` убрать самостоятельное вычисление. Усиление: забрать у
  `reviewer` Bash (Read/Glob/Grep/Write хватает, если diff в файле) — контракт изоляции станет
  enforcement, а не договорённостью. Снимает исключение из ADR-005 о двойной токенизации.
- **Затрагивает:** `verify.md`, `reviewer.md`, `protocol.md`, `docs/DECISIONS.md` (ADR-004/005).

### B4. Мёртвые поля в `session_state.json`
- **Статус:** открыто
- **Суть.** `artifacts` и `history` объявлены в схеме протокола, но ни одна команда их не
  пишет/не читает (состав артефактов по ADR-005 выводится из git). `status`
  (`draft|review|approved|executing`) упоминается один раз — в гейте Business Spec.
- **Рекомендация.** Убрать `artifacts`/`history` из схемы; `status` либо специфицировать
  (когда какие переходы), либо заменить на реально нужное гейтам — например
  `approvals: {spec, design, warn_ack}`.
- **Затрагивает:** `protocol.md`, `start.md`, `next.md`.

---

## C. Процесс и протокол

### C1. Roadmap Фаз 2–4 живёт в gitignored-файле — риск потери
- **Статус:** открыто
- **Суть.** `.sdx/bundles/` в `.gitignore`, а единственная полная спецификация отложенных
  требований (REQ-LANE-1, REQ-LOOP-1, REQ-CACHE-1, REQ-LEAN-1, REQ-CHECKPOINT-1,
  escalate-тир) — это неверсионируемый
  `.sdx/bundles/upgrade_2026-06-27/sdx-efficiency-automation-2026.bundle.md`.
  `docs/specs/phase1-…` содержит только список имён. Один `rm`/clone — roadmap исчез.
- **Рекомендация.** Промоутить Фазы 2–4 в трекаемый `docs/roadmap.md` (или
  `docs/specs/phases-2-4-deferred.md`) — по собственному правилу Closeout «дельты
  переносятся в постоянные доки».
- **Затрагивает:** `docs/` (новый файл).

### C2. `/sdx:switch` делает `git add -A` с авто-коммитом
- **Статус:** открыто
- **Суть.** Слепой `git add -A && git commit` перед checkout закоммитит в ветку сессии всё,
  что лежало в дереве (мусор, потенциально секреты), с шумным сообщением. Не обработаны
  конфликтные случаи (checkout невозможен, та же ветка).
- **Рекомендация.** Минимум — `git stash push` вместо авто-коммита; лучше — перевести
  параллельные сессии на `git worktree` (одна сессия = свой worktree): убирает класс проблемы
  «переключение через общее дерево». Хуки уже worktree-совместимы (сессия по ветке).
- **Затрагивает:** `switch.md`, `protocol.md`, возможно `start.md`/`archive.md`.

### C3. У patch-трека нет ни одного слоя проверки по умолчанию
- **Статус:** открыто
- **Суть.** На patch: stage-gate прозрачен (сразу Execution), Verification «по запросу»,
  fresh-eyes «опционально», stop-gate no-op без тест-команды. Правка уходит в `main` без
  независимой проверки, только с самооценкой автора.
- **Рекомендация.** На patch-Closeout — обязательный лёгкий fresh-eyes прогон (diff маленький,
  стоимость копеечная; можно моделью подешевле), либо хотя бы обязательный регрессионный тест
  как гейт (сейчас «как правило достаточен», т.е. необязателен).
- **Затрагивает:** `protocol.md`, `archive.md`, `verify.md`, `reviewer.md`.

### C4. Discovery на standard-треке не имеет определённого артефакта
- **Статус:** открыто
- **Суть.** Гейт `/sdx:next` требует «зафиксированных результатов инлайн-исследования», но
  нигде не сказано, где они живут.
- **Рекомендация.** Одна строка в протоколе: «результаты инлайн-Discovery фиксируются
  разделом в `change_note.md`» — гейт становится проверяемым.
- **Затрагивает:** `protocol.md`, `next.md`.

### C5. Тихая деградация хуков не видна пользователю
- **Статус:** открыто
- **Суть.** Хуки деградируют в no-op (не та ветка, нет state, нет jq/conf) — safe-by-default,
  но невидимо: пользователь думает, что enforcement работает, а он выключен.
- **Рекомендация.** Расширить `/sdx:status` блоком «здоровье enforcement»: ветка соответствует
  сессии? `jq` есть? `verify-cmd` активен? conf prod-guard заполнен? (те же проверки, что
  делают хуки, выведенные наружу). Туда же — перечисление всех сессий (сейчас `/sdx:switch`
  требует id, а команды листинга нет).
- **Затрагивает:** `status.md`, возможно отдельный скрипт `sdx-health.sh`.

### C6. `/sdx:backtrack` недоспецифицирован
- **Статус:** открыто
- **Суть.** Шаг 4 — два взаимоисключающих действия без правила выбора («пометь outdated ИЛИ
  перемести в бэкап»); шаг 5 «обнули PLAN.md» — деструктив без зафиксированного правила
  «сначала бэкап» (восстановим через git, но правило стоит выбрать одно).
- **Рекомендация.** Выбрать один вариант и зафиксировать.
- **Затрагивает:** `backtrack.md`.

### C7. Противоречие: ADR-005 требует инкрементальных коммитов сессии ↔ `.claude/sessions/` в `.gitignore`
- **Статус:** открыто
- **Суть.** ADR-005 предписывает: «Артефакты коммитятся в ветку сессии инкрементально
  (поддерживает вывод истории из git и Closeout)» (`docs/DECISIONS.md`, стр. 47). Но
  `.claude/sessions/` внесён в `.gitignore` (стр. 2), т.е. файлы сессии физически не могут
  быть закоммичены — они невидимы для git. Ломается заявленное основание ADR-005 (вывод состава
  артефактов и истории из git) и Closeout-шаг «коммиты в ветку сессии». Сейчас сессии живут
  только на локальном уровне и не переносятся между машинами/участниками команды.
- **Рекомендация.** Проработать в рамках рефакторинга работы с сессиями: перенести сессии с
  локального уровня на уровень видимости командой (снять `.claude/sessions/` из `.gitignore`
  либо выделить трекаемый подкаталог для артефактов, оставив в ignore только эфемерное).
  Согласовать с моделью параллельных сессий из C2 (git worktree): одна сессия = свой worktree,
  артефакты версионируются в ветке `sdx/<id>`.
- **Затрагивает:** `.gitignore`, `docs/DECISIONS.md` (ADR-005), `protocol.md`, `start.md`,
  `switch.md`, `archive.md`. Связано с C2.

---

## D. Экономика (по духу ADR-005)

### D1. `@protocol.md` инжектится каждой командой
- **Статус:** закрыто (сессия `fw-econ-a4d1-20260703`, 2026-07-03) — тонкие команды (`status`, `checkpoint`, `switch`, `backtrack`) ссылаются на протокол текстом без `@`-инжекта; `@` сохранён у команд, которым нужен весь протокол
- **Суть.** Все 13 команд включают полный `protocol.md` (~170 строк) при каждом вызове;
  за сессию с 5–6 командами — заметный повтор.
- **Рекомендация.** Тонкие команды (`status`, `checkpoint`, `switch`, `backtrack`) ссылаются
  на протокол текстом («прочитай при необходимости»); `@`-инжект оставить у команд, которым
  нужен целиком (`start`, `next`, `archive`, `verify`).
- **Затрагивает:** `.claude/commands/sdx/*.md`.

### D2. REQ-CACHE-1 (Фаза 2) остаётся актуальным
- **Статус:** отложено (roadmap)
- Детерминированный порядок сборки контекста зафиксировать до появления fanout-контура,
  как и планировалось. См. C1 (roadmap должен быть в docs/).

---

## E. Предложения по развитию процесса (фидбек пользователя после реального использования, 2026-07-19)

### E1. Груминг / ретроспектива / постмортем как типы сессий
- **Статус:** открыто
- **Суть.** Работа над самим бэклогом (пересмотр приоритетов, актуализация статусов, разбор
  накопленного) сейчас не имеет своего типа сессии и трека — делается «по наитию» вне процесса.
  Аналогично отсутствуют ретроспектива (периодический разбор завершённых сессий → выводы в
  бэклог) и постмортем (разбор инцидента → корневая причина → действия).
- **Рекомендация.** Новые типы сессий `grooming` / `retro` / `postmortem` на общем лёгком
  doc-only треке (Discovery-lite → Update → Closeout, без Execution-кода); шаблоны артефактов
  (`retro.md`, `postmortem.md`); выходы всех трёх — записи бэклога. Зависит от E2. ADR.

### E2. Формализация бэклога: структура, префиксы, команды, волны
- **Статус:** открыто
- **Суть.** Бэклог живёт неструктурированно (этот файл + roadmap в gitignored-бандле);
  нет ID-префиксов по типам, трассировки в документации, команд просмотра, планирования волн.
  Техдолг фиксируется нерегулярно.
- **Рекомендация.** Трекаемый `docs/backlog/` (файл на задачу, YAML frontmatter: id, type,
  status, priority, wave, source-session, links) + индекс; префиксы по типам (FEAT-/BUG-/DEBT-/
  IDEA-/PROC-); команды `/sdx:backlog` (список/фильтры/деталь/add); шаг Closeout «отложенное и
  WARN-находки → DEBT-записи»; поле `wave` для планирования волн. Машиночитаемый frontmatter —
  как интеграционная точка будущего плагина портфельного управления. Миграция находок A*–E*
  этого файла в новый формат. ADR.

### E3. Режим экстремального прототипирования (vibe)
- **Статус:** открыто
- **Суть.** Нужен режим быстрой реализации фичи/эпика «в один промпт»: без церемонии спеков,
  без инкрементальных коммитов (быстрый откат грязного дерева), но с обязательной
  «легализацией» после утверждения прототипа — реверс-инжиниринг дельт в SPEC/DESIGN,
  покрытие тестами, verify, и только затем мёрж.
- **Рекомендация.** Тип сессии `proto` с треком `vibe`: стадия Prototype (stage-gate открыт,
  stop-gate off, дерево не коммитится — осознанное исключение из REQ-SESS-1); гейт утверждения
  (отклонено → `git checkout/clean`, принято → обязательный ретрек в легализацию:
  spec-after по образцу `/sdx:init --existing` → тесты qa → verify → Closeout).
  Инвариант enforcement: сессия трека `vibe` не может пройти `/sdx:archive` без легализации.
  ADR (исключение из ADR-005/REQ-SESS-1 фиксируется явно).

---

## Приоритизированный план сессий

| # | Находки | Трек | Усилие | Комментарий |
|---|---------|------|--------|-------------|
| 1 | A1 + A2 | standard | малое | ✅ Выполнено — `fw-enforce-a1a2-20260703` (тесты qa на Verification; prod-guard conf раньше jq) |
| 2 | C1 | patch | тривиальное | Промоут roadmap в docs/ — до любой потери |
| 3 | B1 + B2 | standard | малое | ✅ Выполнено — `fw-model-aliases-20260702` (алиасы тиров, ADR-008) |
| 4 | A6 | patch | малое | verify-cmd мета-проекта (dogfooding stop-gate) |
| 5 | B3 | standard | малое | diff-в-файл + возможный отзыв Bash у reviewer; ADR-правка |
| 6 | A4 | patch | малое | ✅ Выполнено — `fw-econ-a4d1-20260703` (green-run cache, ADR-011) |
| 7 | A3 | standard/full | среднее | Скрипт стадийных переходов + защита session_state.json. Концептуально самое важное усиление |
| 8 | C2 | standard | среднее | stash/worktree в switch |
| 9 | C7 | standard/full | среднее | Рефакторинг работы с сессиями: `.claude/sessions/` из `.gitignore` ↔ ADR-005 (перенос сессий на уровень видимости командой). Делать в связке с C2 |
| 10 | A5, B4, C3–C6, ~~D1~~ | standard | малое | Пакет документационных и мелких процессных правок (D1 ✅ закрыт в `fw-econ-a4d1-20260703`) |
