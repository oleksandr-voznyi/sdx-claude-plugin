# Постоянное хранилище документов разбора типа `intake`

Сюда на Closeout переносится `intake.md` doc-сессии под именем `<session_id>.md` (ADR-017).
Каталог версионируется этим файлом-заглушкой, пока в нём нет разборов.
