# Implementation Plan: Рефакторинг работы с сессиями (git worktree + tracked artifacts + автоопределение основной ветки)

## Статус реализации
100% (23/23; Слои 1-4 завершены — инфраструктура, команды, документация, приёмочная верификация T20-T22 зелёная 14/14; T23 — памятка самозакрытия готова, применяется в момент Closeout)

## Чек-лист задач

### Слой 1 — Инфраструктура (хуки/скрипты/конфиг)

- [x] T1 [TEST] Написать `test-default-branch.sh` (red): кейсы `origin/HEAD=master`; нет remote + `init.defaultBranch=trunk`; нет remote + только `main`; нет remote + только `master`; пустой репозиторий (last-resort `main`). Файл: `.claude/sdx/hooks/test-default-branch.sh` (новый). DoD: скрипт исполняется, все кейсы падают (хелпера ещё нет) или явно недоступны. REQ: REQ-BRANCH-1/2/3/4. DESIGN: «Псевдокод — `lib/default-branch.sh`», «Тестирование — юнит-тест default-branch.sh».
- [x] T2 [CODE] Реализовать `.claude/sdx/hooks/lib/default-branch.sh` (green по T1) точно по псевдокоду DESIGN: `origin/HEAD` → `init.defaultBranch` (с проверкой существования ветки) → эвристика `main`/`master` → last-resort `main`; `set -uo pipefail`; аргумент `proj_dir` опционален (default `$CLAUDE_PROJECT_DIR`/`.`). Файл: `.claude/sdx/hooks/lib/default-branch.sh` (chmod +x). DoD: T1 зелёный. REQ: REQ-BRANCH-1/4. DESIGN: раздел «Псевдокод изменённых скриптов → default-branch.sh», ADR-010.
- [x] T3 [CODE] Обновить корневой `.gitignore` до точной целевой конфигурации из DESIGN («Схема данных / конфиги»): убрать широкий `.claude/sessions/`; добавить `.sdx/worktrees/`, `.claude/sessions/*/.stopgate.*`, `.sdx/bundles/`, оставить `.claude/settings.local.json`. Файл: `.gitignore`. DoD: содержимое побайтово совпадает с блоком DESIGN (с точностью до комментариев); правка + ручная сверка (не TDD — конфиг-файл). REQ: REQ-SESS-1/2. DESIGN: «Схема данных / конфиги».
- [x] T4 [TEST] Точечная проверка косвенной зависимости stop-gate: расширить/добавить кейс в `test-stop-gate.sh` (или отдельный smoke-тест), подтверждающий, что после срабатывания `stop-gate.sh` файлы `.claude/sessions/<id>/.stopgate.count`/`.stopgate.out` действительно не попадают в `git status --porcelain` при новом `.gitignore` из T3 (симуляция tracked-репо, без широкого игнора каталога). Файл: `.claude/sdx/hooks/test-stop-gate.sh`. DoD: новый кейс зелёный после T3; явно не трогает код `stop-gate.sh` (DESIGN: правок не требует). REQ: REQ-SESS-2. DESIGN: «Инфраструктура — UNCHANGED stage-gate/stop-gate, косвенная зависимость».
- [x] T5 [TEST] Переписать существующие сценарии 1–6 `test-archive-verify.sh` под tracked-фикстуру: убрать `printf '.claude/sessions/\n' > .gitignore` из `setup_clean_repo()`; коммитить `.gitignore` с targeted-паттернами; трекать содержательные файлы сессии. Сохранить смысл сценариев (dirty tree, unmerged, success, branch-absent, override, anchored-suffix). Файл: `.claude/sdx/hooks/test-archive-verify.sh`. Ожидаемо RED относительно ещё не переписанного `archive-verify.sh` (использует хардкод `main` и `rm -rf`) — фиксируем как переходное состояние. REQ: REQ-SESS-3, REQ-BRANCH-2. DESIGN: «Тестирование — MODIFIED setup_clean_repo()».
- [x] T6 [TEST] Добавить `setup_worktree_repo()` + Сценарий 7 (tracked-файлы + вариант A, happy path: мёрж → инвариант 6 проходит → `git worktree remove` → `git worktree list` пуст → ветка удалена → `[OK]`) в `test-archive-verify.sh`. Зависит от T2 (использует `default-branch.sh` косвенно через хук) и T5 (общая инфраструктура фикстур). Файл: `.claude/sdx/hooks/test-archive-verify.sh`. DoD: тест написан и запускается (ожидаемо RED до T8). REQ: REQ-WT-1, REQ-WT-5, REQ-SESS-3/4. DESIGN: «Тестирование — Сценарий 7».
- [x] T7 [TEST] Добавить Сценарий 8 (пропущен `git rm` до мёржа → инвариант 6 `[FAIL]`, worktree НЕ удалён) и Сценарий 9 (`git init -b master`, полный вариант-A цикл → инвариант 5 проходит на `master` без `SDX_ARCHIVE_NO_BRANCH_OK`) в `test-archive-verify.sh`. Параллелизуемо с T6 (независимые сценарии в том же файле — можно писать одним PR-коммитом вместе с T6). Файл: `.claude/sdx/hooks/test-archive-verify.sh`. DoD: тесты написаны (ожидаемо RED до T8). REQ: REQ-SESS-3, REQ-BRANCH-2. DESIGN: «Тестирование — Сценарии 8, 9».
- [x] T8 [CODE] Переписать `.claude/sdx/hooks/archive-verify.sh` по псевдокоду DESIGN (green по T5/T6/T7): резолв `def` через `lib/default-branch.sh` (зависит от T2); инвариант 6 — каталог сессии не tracked в дереве `$def`; замена `rm -rf` на `git worktree remove --force` по обнаружению пути через `git worktree list --porcelain`; страховочный `rm -rf` остаточного каталога; `git branch -d` после успеха. Файл: `.claude/sdx/hooks/archive-verify.sh`. DoD: весь сьют `test-archive-verify.sh` (сценарии 1–9) зелёный. REQ: REQ-BRANCH-2, REQ-SESS-3, REQ-WT-5. DESIGN: «Псевдокод изменённых скриптов → archive-verify.sh».
- [x] T9 [TEST] Прогон полного сьюта хуков зелёным одной командой: `test-default-branch.sh`, `test-archive-verify.sh`, `test-stop-gate.sh`, `test-stage-gate.sh`, `test-prod-guard.sh`. DoD: все зелёные, нет регрессий в неизменённых сьютах. Зависит от T1–T8. REQ: сквозная верификация всех REQ-SESS-*/REQ-WT-*/REQ-BRANCH-*, покрытых хуками.

### Слой 2 — Команды `/sdx:*` (prose, правка + ручная сверка — TDD неприменим)

- [x] T10 [DOC] Переписать `.claude/commands/sdx/start.md`: `git worktree add .sdx/worktrees/<id> -b sdx/<id>` вместо `git checkout -b`; seed session_state.json/session.log пишутся в worktree и коммитятся на ветку (`git -C .sdx/worktrees/<id> add/commit`); хендофф-инструкция «продолжить сессию из CLI, запущенного в `.sdx/worktrees/<id>/`». Может выполняться параллельно с T11–T13 (независимые файлы), но логически после T3 (нужен целевой `.gitignore`, чтобы `.sdx/worktrees/` не тянулось в индекс). Файл: `.claude/commands/sdx/start.md`. DoD: текст соответствует Sequence 1 DESIGN дословно по шагам; правка + ручная сверка по чек-листу Sequence 1. REQ: REQ-WT-1, REQ-SESS-1. DESIGN: «Компоненты — start.md», «Sequence 1».
- [x] T11 [DOC] Полностью переписать `.claude/commands/sdx/switch.md` по Sequence 3: убрать `git add -A`/`git commit`/`git checkout`; вывод `git worktree list`, поиск пути worktree по ветке `sdx/<id>`; инструкция открыть/запустить CLI в найденном каталоге; явный текст «[REMOVED] слепой авто-коммит». Файл: `.claude/commands/sdx/switch.md`. DoD: в тексте отсутствуют упоминания `git add -A`/`git commit`/`git checkout` в логике переключения (грепом); правка + ручная сверка. REQ: REQ-WT-2. DESIGN: «Компоненты — switch.md», «Sequence 3».
- [x] T12 [DOC] Переписать `.claude/commands/sdx/archive.md` под вариант A по Sequence 2 (Фаза 1 в CLI сессии: перенос дельт → commit → архив PLAN.md → лог → `git rm -r .claude/sessions/<id>` + commit → хендофф; Фаза 2 в основном CLI: резолв `def` через `lib/default-branch.sh` → `git checkout $def` → `git merge --no-ff sdx/<id>` → `archive-verify.sh <id>`). Зависит от T2 (хелпер), T8 (переписанный хук), T10 (согласованность с моделью start). Файл: `.claude/commands/sdx/archive.md`. DoD: шаги 1:1 со Sequence 2, явно разведены «коммит дельт» и «коммит удаления»; правка + ручная сверка. REQ: REQ-SESS-3/4, REQ-WT-5, REQ-BRANCH-2. DESIGN: «Компоненты — archive.md», «Sequence 2».
- [x] T13 [DOC] Обновить `.claude/commands/sdx/verify.md`: diff через `$(default-branch.sh)` вместо литерала `main` (зависит от T2); исключить `.claude/sessions/**` из diff pathspec для reviewer. Файл: `.claude/commands/sdx/verify.md`. DoD: нет хардкода `main` для сравнения веток; diff-команда содержит `-- . ':!.claude/sessions/**'` (или эквивалент); правка + ручная сверка. REQ: REQ-BRANCH-3. DESIGN: «Компоненты — verify.md», «Обработка ошибок — Reviewer-diff замусорен».
- [x] T14 [DOC] Обновить `.claude/commands/sdx/init.md` шаг 2: убрать добавление `.claude/sessions/` в `.gitignore`; прописывать targeted-паттерны из T3 (worktree-модель); убрать предпосылку «весь каталог сессий игнорируется». Файл: `.claude/commands/sdx/init.md`. DoD: текст init.md порождает `.gitignore`, идентичный конфигурации T3; правка + ручная сверка. REQ: REQ-BRANCH-4 (init без ручной настройки), REQ-SESS-1. DESIGN: «Компоненты — init.md».
- [x] T15 [DOC] (факультативно, не блокирует приёмку) Обновить `.claude/commands/sdx/status.md`: листинг активных сессий через `git worktree list`. Параллелизуемо с любой другой задачей слоя 2. Файл: `.claude/commands/sdx/status.md`. DoD: текст добавляет шаг вывода `git worktree list` в листинг статуса; правка + ручная сверка. REQ: вне обязательных REQ (SPEC «Вне объёма», факультативное улучшение). DESIGN: «Компоненты — status.md (факультативно)».

### Слой 3 — Документация фреймворка

- [x] T16 [DOC] Добавить в `docs/DECISIONS.md` полные тексты ADR-009 («git worktree + версионируемые артефакты сессии») и ADR-010 («Автоопределение основной ветки вместо хардкода `main`») дословно по разделу DESIGN «ADR». ADR-005 не переписывается задним числом (прецедент ADR-008). Файл: `docs/DECISIONS.md`. DoD: оба ADR присутствуют с разделами Контекст/Решение/Обоснование/Инварианты/Связь; правка + ручная сверка. REQ: сквозной (обоснование дизайна). DESIGN: «ADR (кандидаты)». Может делаться в любой момент после Design (не зависит от кода), но по факту содержательно завершается на Closeout вместе с T20.
- [x] T17 [DOC] Обновить `.claude/sdx/protocol.md`: новый подраздел «Модель сессии = worktree = ветка»; переписать §Enforcement-слой под вариант A; переписать §Контракт закрытия (порядок: удаление ДО мёржа); заменить литерал `main` на «основную ветку (автоопределяется)» в §Fresh-eyes, §Контракт закрытия, §Git-флоу. Логически после T8/T10-T12 (описывает уже финализированное поведение). Файл: `.claude/sdx/protocol.md`. DoD: грепом нет непомеченных литералов `main` как имени ветки в затронутых секциях; новый подраздел присутствует; правка + ручная сверка. REQ: REQ-BRANCH-1, REQ-SESS-3/4, REQ-WT-*. DESIGN: «Документация — protocol.md».
- [x] T18 [DOC] Обновить `CLAUDE.md` §6: заменить строку «`.claude/sessions/`: (Игнорируется git через `.gitignore`)» на описание worktree/tracked-модели. Файл: `CLAUDE.md`. DoD: противоречие устранено; правка + ручная сверка. REQ: когерентность триады (описательная синхронизация). DESIGN: «Документация — CLAUDE.md §6».
- [x] T19 [TEST] Грепом по `.claude/sdx/hooks/*.sh` и `.claude/commands/sdx/*.md` подтвердить отсутствие хардкода `main` как имени основной ветки (за исключением явно помеченных иллюстративных упоминаний) — приёмочный критерий REQ-BRANCH-1 из SPEC. Не отдельный файл — контрольная процедура (можно оформить как shell-однострочник, зафиксировать вывод в verification_report.md на этапе Verification). Зависит от T2, T8, T10–T14, T17. DoD: поиск не находит функциональных хардкодов. REQ: REQ-BRANCH-1.

### Слой 4 — Приёмочная верификация (Execution/Verification)

- [x] T20 [TEST] Эмпирическая проверка REQ-WT-1 (приёмочный критерий линчпин-допущения DESIGN §«Три опорных инварианта», п.1 / SPEC §Риски): выполнить реальный `/sdx:start` на тестовой сессии, подтвердить `git worktree list` содержит запись `sdx/<test-id>`; запустить хук (например `stage-gate.sh` или `archive-verify.sh` в dry-режиме) из CLI, реально находящегося в каталоге worktree, и убедиться, что `$CLAUDE_PROJECT_DIR`/резолв сессии указывают на worktree, а не на repo-root. Зависит от T10 (переписанный start.md). DoD: результат зафиксирован (лог/скриншот) как эмпирическое доказательство или опровержение консервативного допущения §8; при опровержении — эскалация решения (см. риск 1 DESIGN). REQ: REQ-WT-1, REQ-WT-4.
- [x] T21 [TEST] Сверка REQ-BRANCH на `master`-фикстуре в реальном (не юнит-тестовом) прогоне: тестовый репозиторий `git init -b master`, полный цикл `patch`-сессии (`start` → `execution` → `archive`) через переписанные `start.md`/`archive.md`/`verify.md`, подтвердить прохождение инварианта 5 `archive-verify.sh` без `SDX_ARCHIVE_NO_BRANCH_OK` и корректный diff в `verify.md`. Зависит от T2, T8, T10–T13 (весь цикл команд), T9 (юнит-сьют уже зелёный). DoD: сквозной прогон зелёный, критерии приёмки REQ-BRANCH-2/3 из SPEC выполнены буквально. REQ: REQ-BRANCH-2/3.
- [x] T22 [TEST] Сквозной прогон REQ-SESS/REQ-WT: `start` → несколько `next`/`checkpoint` → `archive` на дефолтном (`main`) тестовом репо; подтвердить в git-логе `sdx/<id>` коммиты с `session_state.json`/`session.log`/хотя бы одним `.md`-артефактом (REQ-SESS-1); `git status` внутри worktree не показывает `.stopgate.*` как кандидатов (REQ-SESS-2, использует T4); после archive — `git ls-tree -r <default>` не содержит `.claude/sessions/<id>/*`, а `git log <merge>^2 -- .claude/sessions/<id>` показывает историю (REQ-SESS-3/4); `git worktree list` не содержит записи закрытой сессии (REQ-WT-5). Зависит от T9, T10, T12, T20. DoD: все перечисленные REQ подтверждены буквально по формулировкам критериев приёмки SPEC.

### Слой 5 — Особый пункт: самозакрытие текущей сессии

- [x] T23 [DOC] Напоминание/чек-лист для Closeout ЭТОЙ сессии (`fw-session-worktree-20260703`): сессия живёт в СТАРОЙ (не-worktree, gitignored) модели — при `/sdx:archive` этой сессии выполнить ГИБРИДНЫЙ порядок по DESIGN §«Самозакрытие текущей сессии»: (1) перенос дельт `SPEC.md`/`DESIGN.md` в `docs/specs/`/`docs/designs/` как обычно; (2) шаг `git worktree remove` — отметить `(n/a)`, worktree не создавался; (3) удаление `.claude/sessions/fw-session-worktree-20260703/` выполнить обычным `git rm`/`rm` вручную, НЕ через новую worktree-логику `archive.md` (которая к этому моменту уже переписана под T12 и рассчитана на worktree-сессии); (4) зафиксировать в ADR-009 (T16), что это последняя сессия старой модели. Не откладывать до этапа Closeout без явной пометки — риск потерять на автопилоте `/sdx:archive` после T12. DoD: пункт учтён явно в подготовке к Closeout этой сессии; ссылка на этот PLAN-пункт добавлена в напоминание перед вызовом `/sdx:archive`. REQ: n/a (процедурный, не функциональный REQ). DESIGN: «Самозакрытие текущей сессии».

## Зависимости между задачами

Критический путь (последовательно):
T1 → T2 → (T3 параллельно с T2, но T3 нужен раньше T10/T14) → T5,T6,T7 (после T2 доступен для сценариев, могут писаться параллельно друг другу, до T8) → T8 (green по T5–T7) → T9 (полный сьют зелёный) → T10,T11,T12,T13,T14 (после T2/T3/T8/T9 доступны как база; T12 дополнительно зависит от T10) → T15 (факультативно, независимо) → T16,T17,T18 (документация, после стабилизации кода/команд T8–T14) → T19 (греп-проверка, после всего слоя 2/3) → T20 (после T10) → T21 (после T2,T8,T10–T13,T9) → T22 (после T9,T10,T12,T20) → T23 (готовится заранее, применяется в момент реального Closeout этой сессии, после того как T12 уже переписан).

Параллелизуемо:
- T1 и черновая правка `.gitignore` (T3) — независимы от друг друга, но обе предшествуют T2/T8 соответственно.
- T5, T6, T7 — три блока сценариев в одном файле `test-archive-verify.sh`, логически независимы друг от друга (можно писать параллельно/одним коммитом), но все — до T8.
- Слой 2 (T10–T14, кроме взаимной зависимости T12→T10): после готовности слоя 1 (T2, T3, T8, T9) команды `start.md`, `switch.md`, `verify.md`, `init.md` можно править параллельно; `archive.md` (T12) — только после `start.md` (T10) для согласованности модели worktree.
- T15 (status.md) — полностью независим, можно делать в любой момент.
- T16 (ADR в DECISIONS.md) — независим от остального слоя 3, можно готовить сразу после Design (текст ADR уже финализирован в DESIGN.md), не дожидаясь кода.
- T18 (CLAUDE.md §6) — независим от T16/T17, можно делать параллельно.

Явно НЕ включены в план (DESIGN: правок не требуют): `stage-gate.sh`, `stop-gate.sh` (кроме проверочного T4), `prod-guard.sh`, `preflight.sh`.
