# Постоянное хранилище документов разбора типа `postmortem`

Сюда на Closeout переносится `postmortem.md` doc-сессии под именем `<session_id>.md` (ADR-017).
Каталог версионируется этим файлом-заглушкой, пока в нём нет разборов.
