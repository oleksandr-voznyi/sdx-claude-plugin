# Постоянное хранилище документов разбора типа `retro`

Сюда на Closeout переносится `retro.md` doc-сессии под именем `<session_id>.md` (ADR-017).
Каталог версионируется этим файлом-заглушкой, пока в нём нет разборов.
