<!--
  Шаблон СЕССИОННОГО артефакта SDX (sdx/templates/retro.md).
  В отличие от остальных файлов sdx/templates/ (per-project конфиги, копируемые /sdx:init в
  НОВЫЕ проекты), это заготовка АКТИВНОЙ СЕССИИ: копируется в каталог сессии на /sdx:start для
  type=retro и на Closeout переносится в docs/history/retro/<session_id>.md (см. sdx/protocol.md,
  профиль флоу `doc`). Заполните разделы ниже; курсивные подсказки — что сюда писать, не часть
  итогового документа — удаляйте их по мере заполнения.
-->

# Retro: <session_id>

- **session_id**: <...>
- **Дата**: <ГГГГ-ММ-ДД>
- **Тип**: retro
- **Вход**: *период (`<с> — <по>`) ИЛИ явный список `session_id`, заданный пользователем на
  старте сессии*

## Период / охват
- Период: <даты> ИЛИ список сессий: <session_id, ...>
- Источники: `docs/history/sessions-log.md` (срез), затронутые записи `docs/backlog/`

*Зафиксируйте, что именно вошло в разбор — точный диапазон дат или явный перечень сессий, чтобы
результат ретроспективы был воспроизводим.*

## Наблюдения
### Что сработало хорошо
*Практики/решения, которые стоит закрепить или повторить в будущих сессиях.*

### Что не сработало / трение
*Где процесс тормозил, создавал лишнюю церемонию или приводил к ошибкам.*

### Повторяющиеся паттерны
*Наблюдения, встретившиеся в нескольких сессиях за период, — сигнал системной, а не разовой
проблемы.*

## Выводы -> Бэклог
*Основной выход ретро-сессии (REQ-DOC-4). Каждое наблюдение, требующее действия, получает
запись `docs/backlog/`; одна строка таблицы = одно наблюдение → одна запись.*

| Наблюдение | Тип | ID | Статус |
|---|---|---|---|

## Открытые вопросы
*Вопросы, не решённые в рамках этой ретроспективы и требующие отдельного разбора или решения
пользователя.*
